
$(document).ready(function() {
    // let item = itemData;

    // let infoDiv = $('#item-info');

    // let name = '<p class="store-name">' + item.name + '</p>';
    // let image = '<img class="store-image" src="' + item.image + '" alt="Store Image">';
    // let addr = '<p><strong>Address:</strong> ' + item.address + '</p>';
    // let website = '<p><strong>Website:</strong> <a href="' + item.website + '" target="_blank">' + item.website + '</a></p>';
    // let rating = '<p><strong>Rating:</strong> ' + item.rating + ' / 5</p>';
    // let phone_number = '<p><strong>Phone Number:</strong> ' + item.phone_number + '</p>';
    // let store_info = '<p>' + item.store_information + '</p>';

    // let subway_lines = '<p><strong>Nearby Subway Lines:</strong> ';
    // for (let i = 0; i < item.nearby_subway_lines.length; i++) {
    //     subway_lines += item.nearby_subway_lines[i];
    //     if (i < item.nearby_subway_lines.length - 1) {
    //         subway_lines += ', ';
    //     }
    // }
    // subway_lines += '</p>';

    // // Append all information to the infoDiv
    // infoDiv.append(name, image, addr, website, rating, phone_number, store_info, subway_lines);
});
